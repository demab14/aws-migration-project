aws_region             = "eu-west-2"
project_name           = "wp-migration"
admin_cidr             = "203.0.113.4/32"
migration_bucket_name  = "wp-migration-dump-CHANGE-ME"
key_name               = "my-ec2-keypair"
db_username            = "wpadmin"
# db_password is no longer set here — it's generated by random_password
# and stored in AWS Secrets Manager (see terraform/modules/secrets).
